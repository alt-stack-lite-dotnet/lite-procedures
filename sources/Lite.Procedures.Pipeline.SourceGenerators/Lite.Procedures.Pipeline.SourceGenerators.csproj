<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <DevelopmentDependency>true</DevelopmentDependency>
    <IsPackable>true</IsPackable>
    <!-- Analyzer package has no lib/pdb, so a symbol package would be empty (NU5017). -->
    <IncludeSymbols>false</IncludeSymbols>
    <PackageId>Lite.Procedures.Pipeline.SourceGenerators</PackageId>
    <Description>Source generator for Lite.Procedures pipelines (codegen when chain is attribute-only).</Description>
    <NoPackageAnalysis>true</NoPackageAnalysis>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.11.0" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="3.11.0" PrivateAssets="all" />
  </ItemGroup>

  <!-- Ship the generator assembly in the analyzer path so consumers actually run the codegen.
       IncludeBuildOutput=false keeps it out of lib/; this puts it where Roslyn loads analyzers.
       $(TargetPath) is the absolute path to the built dll, so it resolves even on no-build packs. -->
  <ItemGroup>
    <None Include="$(TargetPath)" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
  </ItemGroup>

</Project>
